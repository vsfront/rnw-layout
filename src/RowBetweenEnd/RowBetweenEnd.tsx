import styled from "styled-components";
import { View } from "react-native";

export const RowBetweenEnd = styled(View)`
  flex-direction: row;
  justify-content: space-between;
  align-items: flex-end;
`;
